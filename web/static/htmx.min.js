var script = document.createElement('script');
script.src = "https://unpkg.com/htmx.org@1.9.10";
script.onerror = function() {
    console.warn("CDN HTMX failed to load, fallback to local HTMX initialization.");
};
