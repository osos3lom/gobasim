# Compiled binaries
*.exe
sawt-gateway

# Local environment / secrets
.env
.env.*
# ...but the placeholder template must be tracked (onboarding depends on it).
!.env.example

# Local STT models
models/

# Node (Tailwind CSS build only — web/static/app.css is committed)
node_modules/

# Runtime artifacts (voice-note spool dir)
voice-spool/

# Test coverage output
coverage.out
coverage

# Local scratch space (throwaway binaries, experiments)
scratch/

# Editor/OS noise
.DS_Store
Thumbs.db
